services:
  app:
    entrypoint: |
      bash -c  "alembic upgrade head && \
                uvicorn main:app --reload --port 8000 --host 0.0.0.0"
    volumes:
      - "./:/code/"
    environment:
      DEBUG: True