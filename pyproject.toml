[tool.poetry]
name = "boilerplate-starlette"
version = "0.1.0"
description = ""
authors = ["Vincent Vatelot <vincent.vatelot@ik.me>"]
readme = "README.md"
packages = [{include = "boilerplate_starlette"}]

[tool.poetry.dependencies]
python = "^3.10"
uvicorn = "^0.22.0"
jinja2 = "^3.1.2"
sqlmodel = "^0.0.8"
python-multipart = "^0.0.6"
fastapi = "^0.95.2"


[tool.poetry.group.dev.dependencies]
aiosqlite = "^0.19.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
