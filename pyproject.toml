[tool.poetry]
name = "fastapi-boilerplate"
version = "0.1.0"
description = ""
authors = ["Vincent Vatelot <vincent.vatelot@ik.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
uvicorn = ">=0.22,<0.24"
jinja2 = "^3.1.2"
sqlmodel = "^0.0.8"
fastapi = { extras = ["all"], version = ">=0.95.2,<0.100.0" }
alembic = "^1.11.1"
gunicorn = ">=20.1,<22.0"

[tool.poetry.group.dev.dependencies]
aiosqlite = "^0.19.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
